# sdk-ts (placeholder)

TypeScript SDK for the octo-auth contract.

**Status**: not yet started. Contributions welcome — see the
`contract/auth-v1.yaml` for the wire surface to implement and `sdk-go/`
for the reference design (cache + middleware + scope + RequireSpaceMember
fail-closed semantics).

When this gets started, tags will follow the `sdk-ts/v*` prefix in line
with the parent repo's release convention.
